# Copy to .env only when optional Langfuse observability is needed.
# The shipped product uses local Ollama generation and requires no API key.
# .env is gitignored. Never commit populated values.

# Leave blank to use local RunManifests only.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=
